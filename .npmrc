# Aggressively prevent canvas installation
legacy-peer-deps=true
optional=false
# Don't install optional dependencies
save-optional=false
# Ignore scripts that might install canvas
ignore-scripts=false
