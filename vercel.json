{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "node scripts/install.js || npm install --no-optional --legacy-peer-deps --ignore-scripts || true",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    }
  }
}
