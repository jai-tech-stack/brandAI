{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "rm -f package-lock.json && npm install --no-optional --legacy-peer-deps --ignore-scripts || npm install --no-optional --legacy-peer-deps",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    }
  }
}
