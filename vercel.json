{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "node scripts/block-canvas.js && npm install --no-optional --legacy-peer-deps",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    }
  }
}
